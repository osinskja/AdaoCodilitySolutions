CREATE TABLE IF NOT EXISTS SOLUTION (
    ID VARCHAR(36) NOT NULL PRIMARY KEY,
    TITLE VARCHAR(50) NOT NULL,
    PROBLEM_DESCRIPTION VARCHAR(600) NOT NULL,
    CODE VARCHAR(2000) NOT NULL,
    SOLUTION_DESCRIPTION VARCHAR(600)
)